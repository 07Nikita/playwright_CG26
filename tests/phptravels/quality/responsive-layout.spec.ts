import { test, expect } from '@playwright/test'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; const data=JSON.parse(readFileSync(resolve(__dirname,'../../../test-data/phptravels.data.json'),'utf8'));
test.describe('Accessibility, Security, and Responsive Behavior',()=>{test('Verify responsive layout at mobile and desktop sizes',async({page})=>{for(const viewport of [data.viewports.mobile,data.viewports.desktop]){await page.setViewportSize(viewport); await page.goto(data.application.baseUrl); await expect(page.getByRole('heading',{name:data.home.heroHeading})).toBeVisible(); await expect(page.getByRole('contentinfo')).toBeVisible();}});});
