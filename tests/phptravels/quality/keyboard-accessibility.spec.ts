import { test, expect } from '@playwright/test'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; const data=JSON.parse(readFileSync(resolve(__dirname,'../../../test-data/phptravels.data.json'),'utf8'));
test.describe('Accessibility, Security, and Responsive Behavior',()=>{test('Operate primary controls with keyboard and inspect accessible names',async({page})=>{await page.goto(data.application.baseUrl); await page.keyboard.press('Tab'); await expect(page.locator(':focus')).toBeVisible(); await page.keyboard.press('Escape'); await expect(page.getByRole('heading',{name:data.home.heroHeading})).toBeVisible(); await expect(page.getByRole('tab',{name:/Stays/i})).toBeVisible();});});
