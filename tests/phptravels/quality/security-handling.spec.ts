import { test, expect } from '@playwright/test'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; const data=JSON.parse(readFileSync(resolve(__dirname,'../../../test-data/phptravels.data.json'),'utf8'));
test.describe('Accessibility, Security, and Responsive Behavior',()=>{test('Verify HTTPS and sensitive-data handling',async({page})=>{await page.goto(data.application.baseUrl); expect(new URL(page.url()).protocol).toBe('https:'); for(const path of [...data.application.anonymousProtectedPaths,data.application.loginPath]){await page.goto(data.application.baseUrl+path); expect(new URL(page.url()).protocol).toBe('https:'); await expect(page.locator('body')).not.toContainText(new RegExp(data.expectedMessages.prohibitedSensitiveText,'i'));} await expect(page.locator('body')).not.toContainText(new RegExp(data.expectedMessages.unsafeMarkup,'i'));});});
