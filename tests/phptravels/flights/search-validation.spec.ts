import { test, expect } from '../fixtures'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; const data = JSON.parse(readFileSync(resolve(__dirname, '../../../test-data/phptravels.data.json'), 'utf8'));
test.describe('Other Travel Services', () => { test('Validate flight search controls and incomplete submission', async ({ page }) => { await page.goto(data.application.baseUrl); await page.getByRole('tab', { name: /Flights/i }).click(); await expect(page.getByRole('tabpanel')).toBeVisible(); await page.getByRole('button', { name: /search/i }).click(); await expect(page.locator('body')).toContainText(new RegExp(data.expectedMessages.validation, 'i')); }); });
