import { test, expect } from '../fixtures'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; const data=JSON.parse(readFileSync(resolve(__dirname, '../../../test-data/phptravels.data.json'), 'utf8'));
test.describe('Other Travel Services',()=>{test('Search flights with valid criteria',async({page})=>{await page.goto(data.application.baseUrl); await page.getByRole('tab',{name:/Flights/i}).click(); await expect(page.locator('body')).not.toContainText(/token|stack trace/i);});});
