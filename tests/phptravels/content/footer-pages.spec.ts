import { test, expect } from '@playwright/test'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; const data=JSON.parse(readFileSync(resolve(__dirname,'../../../test-data/phptravels.data.json'),'utf8'));
test.describe('Footer, Information, and Support Pages',()=>{test('Open company, support, explore, and legal pages',async({page})=>{for(const path of data.application.policyPaths){await page.goto(data.application.baseUrl+path); await expect(page.locator('h1,h2').first()).toBeVisible(); await expect(page.locator('body')).not.toContainText(/stack trace|uncaught exception/i);}});});
