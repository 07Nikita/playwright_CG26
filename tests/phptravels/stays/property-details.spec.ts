import { test, expect } from '../fixtures'; import { readFileSync } from 'node:fs'; import { resolve } from 'node:path';
const data = JSON.parse(readFileSync(resolve(__dirname, '../../../test-data/phptravels.data.json'), 'utf8'));
test.describe('Stay Search and Property Discovery', () => { test('Open featured property details', async ({ page }) => { await page.goto(data.application.baseUrl); const notice=page.getByRole('button',{name:data.demoNotice.acknowledgeButton}); if(await notice.isVisible().catch(()=>false)) await notice.click(); const property = data.home.featuredProperties[0]; await page.locator(`a[href*="${property.detailsPathFragment}"]`).first().click({force:true}); await expect(page.locator('body')).toContainText(new RegExp(property.name, 'i')); await expect(page.locator('body')).not.toContainText(/stack trace|uncaught exception/i); }); });
